# Server Configuration
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=buildapp_dev
DB_USER=buildapp_user
DB_PASSWORD=buildapp_password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_min_32_characters_long_please_change_this
JWT_EXPIRE=7d

# OTP Configuration
OTP_EXPIRY=5m

# Notifications (development)
NOTIFICATIONS_ENABLED=false
SMS_PROVIDER=console

# API Configuration
API_URL=http://localhost:3001

# Redis Configuration (optional - for caching)
# REDIS_URL=redis://localhost:6379
# Leave commented out to disable caching (will work fine without Redis for MVP)
